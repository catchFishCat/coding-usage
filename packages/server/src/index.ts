/**
 * @coding-usage/server
 *
 * HTTP server and daemon for coding-usage monitor.
 * Exposes REST API, serves static web files, and runs background scheduler.
 */

/**
 * Placeholder server entry point
 */
export const placeholder = true;
